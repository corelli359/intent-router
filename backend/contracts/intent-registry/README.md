# intent-registry-contracts

Shared Pydantic models used by `admin-service` and `router-service`.

## Local install

```bash
python -m pip install backend/contracts/intent-registry
```

## Package

- import root: `intent_registry_contracts`
- source of truth: `backend/contracts/intent-registry/src/intent_registry_contracts`
