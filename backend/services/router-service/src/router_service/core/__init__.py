"""Core packages for the intent router runtime.

Canonical subpackages:

- ``shared``: shared runtime models
- ``support``: infrastructure clients and stores
- ``prompts``: prompt templates
- ``recognition``: intent/domain recognition pipeline
- ``slots``: slot extraction and validation pipeline
- ``graph``: graph planning and runtime execution
"""
